# Robustez — pós go-live (fluxo do técnico)

> Backlog de endurecimento levantado no **double-check do fluxo do técnico** (2026-06-14).
> **Nada aqui trava o técnico no app** — são lacunas de integridade/robustez a fechar
> **depois** do go-live de segunda. O bug visível (F1 — `data_tarefa` em UTC) já foi
> corrigido à parte. Severidade: 🟠 suja dado · ⚪ cosmético · 🔓 trava só no app (burlável por API direta).

## Pacote principal (fechar primeiro depois do go-live)

### F2 — Travas de negócio só existem no app (burláveis no servidor) 🟠🔓
- **Onde:** o sync escreve direto na tabela com o JWT do técnico (`js/sync.js:73`); não há Edge/trigger/CHECK validando conteúdo de `rats`/`tarefas`. As travas vivem só em JS: concluir exige RAT `registrado` + sem passagem aberta (`js/tecnico.js:657-680`), tempo improdutiva (`:2999`), cronologia (`:2744-2768`), término-futuro (`:2729-2737`), passagem falta+levar (`:2930-2933`).
- **Risco:** por API direta dá pra marcar tarefa `concluida` **sem nenhuma RAT**, ou subir RAT com horários incoerentes. No app está tudo travado (e os botões somem) — o risco é cliente malicioso/bug de integração, não o técnico.
- **Correção:** `trigger validar_rat() BEFORE INSERT/UPDATE on rats` (cobre tempo/cronologia/passagem, replicando o tratamento de rollover/dia-anterior do app pra não rejeitar RAT de madrugada) + `trigger validar_conclusao_tarefa() BEFORE UPDATE on tarefas` (exige RAT `registrado`/`concluida*` e barra passagem aberta, **com isenção via `app_role()`** pro admin forçar com ciência). **Migration em prod — revisar antes, com as duas provas (positiva/negativa), no padrão de sempre.**

### F3 — Sem identidade determinística de RAT + sem unique (tarefa, dia) 🟠
- **Onde:** `js/db-local.js:149` gera `client_uuid` **aleatório** (não o UUIDv5 de `(tarefa_id, dia)` que o CLAUDE.md prevê). No servidor o único unique é `(tarefa_id, rat_seq)` (migration 0045) — **não** `(tarefa_id, dia)`.
- **Risco:** a deduplicação "uma RAT por (tarefa, dia)" depende 100% do `ratDoDiaDe` no app; dois aparelhos/coautores (ou uma corrida) criam dois `client_uuid` diferentes pro mesmo dia → duas RATs. É a raiz das RATs duplicadas.
- **Correção:** gerar `client_uuid = UUIDv5(namespace, "${tarefa_id}|${diaISO}")` na criação da RAT, pros aparelhos convergirem no upsert por `client_uuid`; + índice único `(tarefa_id, dia)` no servidor como rede. Atenção à migração de RATs já existentes.

### F4 — (faz parte do F2) validação de RAT no servidor
Coberto pela `validar_rat()` do F2. Mantido aqui só como lembrete de que tempo improdutiva / cronologia / término-futuro / passagem precisam do equivalente server-side.

### F5 — `ratDoDiaDe` não reusa rascunho não salvo ✅ RESOLVIDO (2026-07-20)
- **Era:** `ratDoDiaDe` exigia `status === 'em_andamento' || 'registrado'`; `novoRat` nasce sem status e o rascunho do dia ficava invisível pro dedup — 2º toque no card criava RAT duplicada. **Materializou em produção na Tarefa 4852** (2 RATs no dia 14/07, horas sobrepostas; dados consolidados nas migrações 0120/0121).
- **Fix:** commit `c3272e7` (SW v666) — status ausente conta como `em_andamento` (`r.status || 'em_andamento'`); comentário alinhado. Improdutiva/concluída seguem fora do reuso. Verificado com teste do predicado real (8 cenários).

### F12 — Alerta de integridade "Concluída sem RAT registrada" (portal admin) 🟠
- **Motivo:** hoje a trava de "exige RAT registrada pra concluir" só roda no **momento** de concluir (e o app esconde o botão). Não há **alerta retroativo**: se uma tarefa ficar concluída/concluida_pendencia com **0 RAT registrada** (burla de API — ver F2 —, "admin força", ou churn de dados), nada aponta. É o irmão retroativo do F2.
- **Correção:** no portal admin (`js/tarefa.js`), ao renderizar a tarefa, se `status ∈ (concluida, concluida_pendencia, aprovada_faturamento, faturada)` **e** nº de RATs com `status ∈ (registrado, concluida, concluida_pendencia)` = 0 → badge "Concluída sem RAT registrada — verificar". Leitura pura (o portal já carrega `cur.rats`), **sem migration**. Pode espelhar na tela de **Conciliação**.
- **Nota:** levantado a partir da 04757, que na verdade **tinha** a `/02 registrado` no servidor — o "sem RAT" era view local (ver F13). O alerta vale pro caso de órfã real.

### F13 — Lista de RATs do técnico lê só o aparelho (local-only) 🟠 (baixo)
- **Onde:** `js/tecnico.js:787` `renderRatsDaTarefa` usa `D().listarRats()` (IndexedDB) e esconde a seção se vazia (`:789`). O técnico **não vê** RATs que estão só no servidor (de coautor, ou após excluir a cópia local) — mesmo o gate de concluir já consultar o servidor (autoritativo). Tela fica enganosa ("parece sem RAT").
- **Correção:** `renderRatsDaTarefa` buscar também do servidor (merge com o local por `client_uuid`), como o bloco de conclusão já faz. Mudança de comportamento no app — testar offline (cair pro local quando sem rede).

### F15 — Estado de erro nas demais listas do portal (erro != vazio) 🟠 (baixo)
- **Motivo:** mesma "mentira silenciosa" do F14 (já corrigido p/ RATs): `lista = error ? [] : data` + render de vazio idêntico a "deu erro". Um erro transitório (rede/RLS/versão) some como "nada aqui".
- **Onde (todos mudos — sem toast/aviso):**
  - `js/tarefa.js:572` `carregarEquip` → "Nenhum equipamento vinculado."
  - `js/tarefa.js:603` `carregarAnexos` → "Nenhum anexo." (**também alimenta o card Situação "Anexos: Nenhum"** — mente no resumo, como o RATs mentia).
  - `js/tarefa.js:1345` `carregarTimeline` → "Sem eventos registrados ainda."
  - `js/painel.js:39` `renderRecentes` (dashboard) → "Nenhuma RAT ainda."
- **Já OK (não mexer):** `js/tarefa.js:646` `carregarLinhas` (conciliação) já dá `toast('Erro ao carregar conciliação…')` (`:648`).
- **Correção:** mesmo padrão do F14 — flag de erro por lista, render "Erro ao carregar — recarregue" em vez do vazio (e card Situação dos Anexos idem). Só frontend, sem migration.
- **Prioridade interna:** anexos > painel/recentes > equipamentos > timeline.

## Cauda (cosmético / baixo — quando sobrar)

- **F6 ⚪ — `js/tecnico.js:28** `T_STATUS` não lista `devolvida`/`aprovada_faturamento`/`faturada` (mitigado por `ref.status` do servidor). Acrescentar as 3 chaves.
- **F7 🟠 (baixo) — `js/sync.js:67`** upsert pula campos `null` → uma RAT improdutiva que vira produtiva não apaga `motivo_improdutiva` no servidor (já mitigado p/ `atendimento_executado` via `true`). Enviar nulls explícitos das colunas "limpáveis".
- **F8 🟠 (baixo) — migration 0062** trigger `rat_inicia_tarefa` é só `after insert`; editar RAT improdutiva→produtiva direto no portal não promove a Tarefa via banco. Adicionar `or update of atendimento_executado`.
- **F9 ⚪ — `js/db-local.js:207`** reabrir RAT `confirmado` é transição inválida que só gera `warn` e aplica mesmo assim (re-sobe; sem perda de dado). Bloquear o `put` ou documentar que reabrir-e-reenviar é intencional.
- **F10 ⚪ — `js/sync.js:48`** foto é marcada `enviada` antes do upsert da RAT → fica órfã no Storage até o retry (converge sozinho). Mover o `relatorio_fotos.upsert` pra logo após cada foto, ou aceitar.
- **F11 ⚪ — `js/tecnico.js:590`** `nt-data` (nova tarefa em campo) usa default `new Date().toISOString().slice(0,10)` (UTC). Trocar por `jorHoje()`.
- **Fora de escopo — `js/tecnico.js:3236`** `data: toISOString()` em **pré-orçamento** (orçamento migrou pro comercial-app; código legado no SR).
- **F16 ⚪ — `rat_seq` reutiliza o número de RAT excluída** (registrado 2026-07-20, caso 4852). `tg_rat_seq` atribui `max(rat_seq)+1` por tarefa (migration 0045): excluir a **última** RAT da tarefa libera o número — a próxima nasce com o mesmo `/NN` da excluída. Caso concreto: a 4852/02 foi excluída na consolidação (migração 0120); a próxima RAT da 4852 nascerá **"/02" de novo**, e trilhas antigas (auditoria/rat_edicoes/backup_0120) que citam "4852/02" passam a ser ambíguas se lidas só pelo número. Mitigação já praticada: as trilhas da 0120/0121 citam sempre `id`/`client_uuid` junto do número. Correção estrutural (se um dia doer): contador persistente por tarefa em vez de `max+1`, pra número de RAT nunca ser reutilizado. **Não bloqueante.**
- **P0 — escalonamento via `usuarios.role` 🟠 Fase A RESOLVIDA (2026-07-20, migração 0124)**. Auditoria de exposição do schema: a policy `usuarios` "Admin update" (`UPDATE TO public USING auth.uid()=id`, sem trava de coluna) + ausência de trigger deixavam qualquer autenticado fazer `update usuarios set role='admin'`, e `portal_is_admin()` mais 5 Edge Functions decidem "admin" lendo `usuarios.role` → cadeia a admin global. **Fase A (0124):** trigger `trg_usuarios_protege` barra alteração de `role/ativo/id/email` por papel não-privilegiado (isenta service_role/postgres/supabase_admin/supabase_auth_admin) + `drop` das policies permissivas "Admin insert"/"Admin update" (o cliente nunca escreve `usuarios` — só Edge service_role). Policies de leitura intactas; Edges não tocadas. Migração atômica auto-abortante; regressão em `supabase/tests/teste_0124_usuarios_lock_privilegios.sql`. **Fase B ✅ (PR isolado, aguardando merge):** `omie-sync`, `viagem-merge` (caminho "escritório") e `orcamento-importar-fotos` passaram a autorizar por `portal_acessos` do app `service_report` (`role_chave in ('admin','gestor_axis')`), não mais por `usuarios.role`. Sem regressão medida: `usuarios.role` só tinha `admin`(4)/`colaborador`(11) em produção — `gestor_axis`/`comercial` nunca casavam ali, então o `comercial` do OFFICE de orcamento-importar-fotos era entrada morta (0 perderiam acesso). O conjunto passa de 4→6 (os 2 SR-admins antes excluídos pelo proxy ruim — ampliação **devida** ao conjunto real de SR-office); caminhos "a bordo"/"criador" do viagem-merge intactos. Gate validado nos 5 perfis (`supabase/tests/teste_p0_faseb_gate_edges.sql`). **`manage-users`/`portal-usuarios` NÃO migradas** (Portal-global; service_report ampliaria de 4→6 admins de gestão de usuários — over-grant): seguem no papel global, agora confiável pela trava da 0124. Demais achados da auditoria (P1: app_secrets no schema public, views definer conciliacao/material, bucket avatars público+escrita livre · P2: sr_usuarios expõe e-mail, clientes completo ao técnico · P3: backup_0120, search_path) seguem no radar, não iniciados.
- **P1a — `app_secrets` fora do schema público 🟠 EM ANDAMENTO (faseado)**. Tabela `public.app_secrets` (chave/valor) guarda 4 segredos vivos (`anthropic_api_key`, `cron_secret`, `vapid_public`, `vapid_private`) com grant a anon/authenticated (RLS deny-all protege, mas é furo de defesa + viola CLAUDE.md). Leitores: 4 Edge Functions (via service_role) **e 2 jobs do pg_cron** (0090/0094) que leem `cron_secret` no SQL agendado para o header `x-cron-secret` → dependência DB-side (o pg_cron não enxerga env). **Destino:** VAPID×2 + anthropic → Function Secrets (`Deno.env`); `cron_secret` → Vault (`vault.decrypted_secrets`, lido pelo pg_cron) **e** Function Secret (lado da Edge). Provisionamento dos secrets é **manual** (Dashboard/CLII) — nenhum valor entra em código/commit/log/backup; rollback usa os valores já no Vault/Function Secrets. **Faseamento:** (1º PR ✅ mergeado + deployado) 4 Edges passam a `Deno.env.get` com fallback TEMPORÁRIO à tabela (zero downtime; smoke: 401 nas chamadas seguras, sem 5xx); (2º PR — aguardando merge, migração 0125) reaponta os 2 cron jobs para `vault.decrypted_secrets`, com guarda auto-abortante que exige o Vault secret existir e bater com o valor atual (comparação booleana, sem expor valor); (3º PR) remove o fallback + revoga acessos; (PR final) `drop table` após ≥1 ciclo dos dois crons. Provisionamento dos Function Secrets e do Vault secret é **manual** (Dashboard/CLI) — o CLI **não está disponível neste ambiente** (não instalado/autenticado/vinculado), então o cadastro é no Dashboard; nenhum valor entra em código/commit/log. Referência: `docs/service-report-spec-completo.md` (cron por `x-cron-secret`).
- **F17 — views definer legíveis por `anon` ✅ RESOLVIDO (2026-07-20, migração 0123)**. O levantamento confirmou o furo em TRÊS views: `vw_participacoes_dia` (anon lia 254 participações), `vw_rats_busca` (80 RATs com `respostas::text` inteiro) e `vw_alerta_desloc_sem_volta` (nasceu invoker na 0065 e **perdeu a opção quando a 0081 fez `create or replace` sem redeclará-la** — o Postgres reseta reloptions no replace). Fix: `security_invoker = true` + `revoke select from anon` nas três, **sem tocar em policy-base** — acesso admin e escopo próprio do técnico vêm do RLS existente. Migração auto-abortante (admin perde acesso / técnico fora do escopo / view sem invoker / anon ainda lendo → rollback); regressão vigiada por `supabase/tests/teste_0123_views_invoker.sql`; regra da casa registrada no CLAUDE.md (todo replace de view redeclara o invoker). Nota de higiene que fica: policies do schema-base de `rats`/`tarefas`/`clientes` existem só no banco (não versionadas — pré-0002).

- **F18 — corrida do ACK: save durante o envio era esmagado ✅ RESOLVIDO (2026-07-21, PR #119, SW v670)**. Caso 04895 (UNIVILLE/Maicon): encerrar a RAT enquanto uma rodada de sync estava em voo (fotos subindo) fazia o ACK do retrato velho marcar `confirmado` por cima do `salvo_local` novo — o encerramento ficava preso no aparelho rotulado de sincronizado (encerrada sexta 15:45, servidor só recebeu segunda 08:49 via nova edição; painel mostrou "Não encerrada · há 3 dias" o fim de semana inteiro, corretamente). Dois defeitos combinados: ACK confirmava incondicionalmente, e `syncAll()` durante rodada em voo era descartado em silêncio — justamente a chamada que o Encerrar dispara. Fix: retrato tirado DEPOIS de marcar `enviando`; ACK só confirma se o status ainda é `enviando` (senão `na_fila` + rodada extra reenvia); CAS `apenasSe` no `definirStatus` (atômico na tx do IndexedDB); `syncAll` re-executa em vez de descartar. Vale pra RAT e pré-orçamento. Verificado por harness em Node rodando o `sync.js` real (save simulado no meio do upload). Vigia de campo: evento `na_fila` com detalhe "alterada durante o envio — reenviar" em `sync_eventos` = corrida pega e resolvida.
- **F19 — RAT de outra conta re-tentava para sempre (RLS 42501) ✅ RESOLVIDO (2026-07-21, PR #120, SW v671)**. Aparelho que trocou de login carrega RAT pendente da conta anterior; o RLS de `rats` (só o dono grava) recusava e o sync re-tentava eternamente — toast "1 item(ns) com erro de envio" a cada sync, sem caminho de solução (visto no aparelho de teste, 21/07 14:39, 6× "new row violates row-level security policy" nos logs). Fix: recusa de RLS marca `envio_bloqueado_rls` + erro com motivo na trilha; item sai do toast e NÃO re-entra na fila sob o login errado; lista rotula "não enviada — RAT de {técnico}; entre com essa conta para enviar"; quando a conta dona loga, volta à fila sozinha e o flag limpa no envio. Nada é apagado (§12). Verificado por harness do `syncAll` real com RLS simulado (ciclo completo). **Encerramento (2026-07-22, PR #121, SW v672):** regras finas por cima do fix — só o 42501 do upsert de `rats` marca bloqueio (42501 em foto/material segue o fluxo comum de erro); estado local mínimo `{em, usuario, provado}` (sem payload, conteúdo de RAT, e-mail ou token); mensagens exatas conforme comprovação de propriedade ("criada por outro usuário… entre com a conta original" / "restrição de acesso… permanece salvo neste aparelho"), registradas uma única vez; nova tentativa só por ação manual (exatamente 1, sem loop) ou troca de login; item nunca sai da fila nem tem `client_uuid`/`tecnico_id` alterados. Validação: 19 gates em Node sobre o `sync.js` real + smoke de duas contas no navegador (9/9 — `sync.js`/`db-local.js` reais da main deployada, IndexedDB real, incluindo reinício do app; a troca de contas foi simulada na camada de auth, pois entrada de credenciais é vedada à automação — um passe físico com as duas contas reais segue disponível se desejado). **A RLS de `rats` permaneceu intacta durante toda a frente e funcionou exatamente como mecanismo de proteção: nenhum dado foi perdido, nenhuma policy foi alterada e nenhum bypass foi criado. Frente encerrada.**
- **F20 — tarefa de orçamento nasce sem tipo de serviço → técnico trava em campo ✅ RESOLVIDO (2026-07-21, direto na main, SW v669)**. Caso 04840 (VALMET): a RPC da trilha comercial (0115) cria a Tarefa sem `tipo_servico_id` (o orçamento não carrega essa informação — inevitável na origem); sem tipo não há formulário e a RAT abre sem campos ("peça ao administrativo"). Fix no ponto de passagem obrigatório: o modal Dados do Portal bloqueia salvar/criar sem tipo (asterisco `.req` no label); pendência e app do técnico já validavam. Estrutural que fica: enquanto o orçamento não tiver tipo de serviço próprio, toda tarefa de orçamento nasce sem tipo até o primeiro salvar no Portal.

- **F21 — iOS mata a conexão do IndexedDB (câmera/segundo plano) → app inteiro aborta ✅ RESOLVIDO (2026-08-05, rodadas 1+2, SW v688; confirmado em campo pelo caso real)**. Caso Arian Camilo (05/08): tocar em Câmera/Galeria no iPhone abre o seletor nativo → o iOS suspende o PWA → o WebKit mata a conexão do IndexedDB por baixo. Ao voltar com a foto, a conexão cacheada (`_dbConn`) era um zumbi: a gravação abortava com `AbortError` ("The operation was aborted.", banner vermelho via unhandledrejection) e — pior — o cache defunto ficava: **toda** operação local seguinte (salvar, encerrar RAT) abortava até recarregar a página. **TERCEIRO caso da mesma família iOS**: (1) blob file-backed invalidado → foto ilegível (caso Maicon, `marcarFotoFalha`); (2) PWA morto no meio do envio → round morto (retry `enviando→na_fila`, F18); (3) agora a conexão IDB morta pela câmera. **Lição de linhagem: recurso cacheado no iOS PWA é mortal; todo caminho precisa de detecção de morte + recuperação.** Fix (`js/db-local.js`): (a) handler `onclose` na conexão (o WebKit dispara na morte anormal; close explícito não) zera o cache com guarda de identidade; (b) `tx()` e leituras avulsas (`ro()`) re-tentam **UMA vez, só** para `InvalidStateError`/`AbortError` de conexão — erro de dado (Constraint/Quota) sobe direto, sem retry; re-rodar o `fn` é seguro porque transação abortada não commitou nada (atômica) e nenhum store usa autoincrement; (c) `adicionarFotos` (`js/tecnico.js`) não vira mais banner de crash — falha residual vira toast "tente de novo" mantendo as fotos que entraram. Verificado por harness em Node com o `db-local.js` real sobre IndexedDB falso com interruptor de morte (`test/db-local-conexao-morta.test.mjs`, 18 gates): os dois modos de morte (zumbi InvalidStateError e abort em voo com `t.error` nulo), recuperação, **idempotência (dado grava UMA vez — inclusive `_registrarEvento`, que gera uuid dentro do fn)** e a prova negativa (Constraint/Quota NÃO retentam). **Varredura de fechamento da família (2026-08-05) — "provei que não há": nenhum outro recurso cacheado matável em aberto.** Inventário: (a) conexão IndexedDB → este F21; (b) upload em voo / PWA morto → F18 (round morto, retry `enviando→na_fila`); (c) blob file-backed invalidado → caso Maicon (`marcarFotoFalha`, foto sinalizada p/ re-anexar, nunca apagada); (d) **câmera/mídia: não existe handle vivo** — captura é `<input type=file capture>` (seletor nativo), zero `getUserMedia`/`MediaStream` no código; (e) **GPS: sem `watchPosition`** (só pontual, por decisão de spec), sem wake lock/AudioContext; (f) **service worker**: registration é persistente do navegador (não é cache de página); morte do processo SW é ciclo de vida normal e o app re-chama `reg.update()` no visibilitychange (`tecnico.html:1781`); (g) **websocket realtime** (`sync.js` `rtChannel`): o iOS mata em segundo plano, mas é só ACELERADOR — a convergência é determinística por `visibilitychange→pullChanges` + intervalo de 2min + `online→syncAll` (`sync.js:637-640`), e o supabase-js reconecta sozinho (heartbeat/backoff); (h) **token de auth**: expira em segundo plano → 1ª chamada pós-retomada pode falhar, mas a fila de sync re-tenta (maquinário F18/F19, nada se perde) e o auto-refresh retoma; (i) **object URLs** das miniaturas: recriadas a cada render a partir do blob do IndexedDB — pior caso é miniatura quebrada (cosmético, cura no sync via preview assinado). Achado colateral (não-iOS, baixo): app aberto OFFLINE nunca liga o realtime — o handler `online` chama só `syncAll()`, não `startRealtime()` (`sync.js:637`); converge mesmo assim pelo pull de 2min, fica como melhoria barata. **Rodada 2 (2026-08-05, SW v688): caso Arian persistiu COM o v687 ativo** — o toast novo apareceu ("3 fotos") e o retry em conexão fresca falhou nas 3: falha DETERMINÍSTICA, não a morte transitória por suspensão. Hipóteses vivas: (a) IDB morto pra sessão inteira da página (WebKit às vezes não reabre até reload); (b) quota/iPhone cheio; (c) blob file-backed ilegível NA GRAVAÇÃO — se `comprimirFoto` cai no fallback (foto grande demais pro canvas → devolve o `File` cru), clonar o file-backed blob pro IDB aborta se o iOS já limpou o arquivo temporário do seletor (irmão do caso Maicon, na escrita). Entregue no v688: **materialização do blob** (`arrayBuffer()` → Blob em memória quando a compressão cai no fallback — elimina (c) na origem e, se o arquivo estiver ilegível, o erro sai NOMEADO, ex.: NotReadableError); **toast-diagnóstico** com nome do erro real + `storage.estimate()` (uso/quota em MB — o print do técnico passa a dizer a causa) + dica de campo por tipo de erro; banner de unhandledrejection mostra o NOME do erro (DOMException não tem stack); `refreshThumbs`/`atualizarResumoFotos` resilientes a leitura falha (fim do banner residual). **Fechamento (2026-08-05, mesma tarde): confirmado em campo** — com o v688, o Arian gravou as 3 fotos sem erro (print). Leitura honesta da causa: o resultado é compatível com (c) corrigida pela materialização, mas o fechar-e-reabrir que trouxe o v688 também renovaria a página (o que curaria (a) transitoriamente) — a causa exata não é discriminável a posteriori. **Vigia de campo que fica:** se o toast de falha reaparecer com `AbortError` mesmo com blob em memória → é a hipótese (a) (IDB morto pra sessão) e a rodada 3 é auto-recuperação por reload; qualquer reincidência agora chega NOMEADA no print (quota/ilegível/abort), então a triagem é imediata.

- **F22 — `tg_rat_seq` calculava o nº da RAT sob o RLS de quem insere → colisão eterna em tarefa multi-técnico ✅ RESOLVIDO (2026-08-05, migração 0138 APLICADA em produção; PR #148)**. Caso Pablo Soares/tarefa 4949 (05/08): 2 RATs presas em loop de "erro de envio" — o log do Postgres mostrava pares de `duplicate key uq_rats_tarefa_seq` a cada rodada de sync. Mecanismo (provado por simulação de RLS em produção): o trigger da 0045 atribui `rat_seq = max(rat_seq)+1` **sem SECURITY DEFINER**, então o `max` roda sob o RLS do técnico que insere; a policy de SELECT de `rats` só mostra as PRÓPRIAS linhas (a 0063 afrouxou só `tarefa_tecnicos` — `rats` nunca ganhou visão de coautor). Pablo via 0 RATs na 4949 (real: 2, do Marcelo Phablo) → seq 1 → colide com linha invisível → determinístico, retry infinito. **É sistêmico**: qualquer segundo-autor de RAT numa tarefa colide. Fix (migração `0138_tg_rat_seq_definer.sql`): função vira SECURITY DEFINER com `search_path` travado (numeração é fato da TAREFA, não de quem insere; precedente 0063 `sou_responsavel_tarefa`); advisory lock mantido; guarda auto-abortante (definer + search_path + trigger apontando). Verificação: `supabase/tests/teste_0138_rat_seq_definer.sql` (padrão 0123, seguro em prod: fixture descartável + RAISE final incondicional) — **prova positiva** (técnico cego pelo RLS insere e recebe o seq global correto, 3 = max real+1) e **prova negativa** (função invoker reproduz o duplicate key) — rodado contra produção em 05/08: `TESTES_OK`, rollback confirmado (`prosecdef=false` após). Consequência prevista pós-fix: as RATs do Pablo entram como seq 3+ — podendo coexistir com RAT do Marcelo no mesmo dia (conflito visível pro admin; é o F3, backlog conhecido, não este bug). **Encerramento em campo (2026-08-05 18:16 UTC):** as 2 RATs presas eram da tarefa **4806** (mesma anatomia do exemplar 4949: Marcelo tinha 4806/1 e /2, invisíveis pro Pablo) — pousaram como **4806/3 e 4806/4** no primeiro sync pós-apply; os pares de `duplicate key` **cessaram no log** exatamente no apply (último par 17:40, apply 17:56, zero depois). Nota de observação: 2×`42501` em `rats` às 18:16:23 (5s antes do sucesso) — transiente da primeira rodada do aparelho recém-aberto (token/claims), autocurado pelo retry; só investigar se virar padrão. **Varredura de fechamento da família (2026-08-05) — "provei que não há": nenhum outro cálculo servidor agrega sob o RLS do chamador devendo agregar sobre a verdade completa.** Método: pg_proc × tabelas com RLS — toda função invoker (sql/plpgsql, public+axis) cujo corpo tem `max/min/sum/count/array_agg/string_agg/exists` E cita tabela com RLS. Resultado: 3 candidatas, todas guardas de validação do módulo Ponto, todas seguras por cobertura de papel: (a) `tg_ponto_fe_valida` e (b) `tg_ponto_map_valida_escopo` — só escrevem nas tabelas-alvo `app_role()='admin'` (policy) ou `service_role` (bypassa RLS), e o admin tem SELECT integral nas tabelas consultadas (`pmap_read`/`pfe_read` sem restrição de linha) → o EXISTS vê a verdade completa pra todo papel que dispara o trigger; (c) `tg_usuarios_tangerino_elegivel` — o EXISTS só roda APÓS o gate `current_user in (service_role, postgres, …)`, que bypassa RLS por definição. **Nota preventiva que fica:** essas guardas são seguras pelo CONJUNTO ATUAL de papéis escritores; se um dia técnico ganhar INSERT nessas tabelas (ou a leitura do admin ganhar restrição de linha), elas entram na classe e precisam do tratamento da 0138 (definer ou gate de papel antes do EXISTS). Regra da casa: **derivação de valor/validação em trigger sobre tabela com RLS → ou SECURITY DEFINER com search_path travado, ou prova explícita de que todo papel escritor enxerga o conjunto completo.**

- **F23 — vigia "RAT presa no aparelho" (fotos órfãs no Storage) ✅ ENTREGUE (2026-08-05, migração 0139 APLICADA em produção)**. Lição operacional do caso 4806 (cauda do F22): uma RAT presa no aparelho era **invisível pro servidor por dias** — os técnicos reclamaram, e o único rastro servidor era a pasta de fotos órfã no bucket (o sync sobe foto ANTES do upsert da RAT, `js/sync.js`; F10 já apontava a órfã transitória). A janela de cegueira agora fecha: `vw_alerta_rat_fotos_orfas` lista toda pasta `{tecnico}/{client_uuid}/…` do `rat-anexos` **sem** linha em `rats`, excluindo o explicável (pré-orçamento no mesmo bucket; tombstone = RAT excluída pós-sync; último upload < 1h = sync em voo). Molde 0122/0065: `security_invoker = true` + gate `app_role() in (admin, gestor_axis)` na própria view + `revoke` de `anon` + guarda auto-abortante; só leitura, reversível com `drop view`. Verificação: `supabase/tests/teste_0139_vw_alerta_rat_fotos_orfas.sql` (5 casos + autorização, rodando as asserções sob `set local role authenticated` com claims de admin real — prova o caminho completo do portal, inclusive grants/RLS de `storage.objects` via `rat_anexos_admin`) — rodado contra produção em 05/08: `TESTES_OK`. **Estado real no apply:** exatamente 3 pastas órfãs, todas do Pablo Soares, todas ANTERIORES ao fix do F22 (01/06 · 4 fotos, 08/06 · 1 foto, 26/06 · 2 fotos) — candidatas a vítimas antigas do F22 cuja cópia local já se foi (o sync de 05/08 esvaziou a fila do aparelho dele e nada mais pousou); triagem com o técnico pendente, fotos preservadas no bucket. As pastas da 4806 saíram sozinhas da view quando as RATs pousaram — comportamento esperado. **Uso: consultar a view (portal ou SQL) quando "técnico diz que preencheu e não aparece"; linha aqui = envio morto no meio ou exclusão só-local.** Card "Envios presos no aparelho" entregue no Painel diário (mesma sessão, SW v689): vermelho (dados de campo em risco), reusa as classes `devol-alert`, só aparece quando há linha; para papéis fora de admin/gestor a view devolve 0 linhas e o card some sozinho.

## Verificado e SÃO (não mexer)
- Invariante "RAT não conclui Tarefa": intacto (`js/sync.js:103` MAP só `→ em_execucao`; nenhum trigger/edge escreve `concluida` por RAT).
- `respostas.data` **é** gravado (template tem campo `data`, default `jorHoje()` local); `ratDoDiaDe` usa a chave certa. (Falso-positivo descartado conferindo no banco.)
- Idempotência offline por `client_uuid`; pull não sobrescreve local pendente; ordem foto→RAT→fotos/materiais correta.
